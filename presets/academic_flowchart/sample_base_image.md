Clean minimalist academic presentation slide background, very light cool gray with a subtle dotted grid, four wide and tall empty rounded rectangle placeholders arranged horizontally in the middle (each rectangle wide enough to comfortably hold a centered label and tall enough to hold it) connected by thin elegant arrows flowing left to right, professional research figure style, generous empty space at top for a title and at bottom for a caption, flat design, no text, no letters, no numbers, no labels, no watermark

Output requirements:
- Generate exactly one image at 2400x1350 pixels (2400 wide x 1350 tall).
- Save the final image as: presets/academic_flowchart/sample_base_image.png
- Enforce exact pixel dimensions if the model produces a non-matching size: use 'sips -z 1350 2400' on macOS, or ImageMagick ('magick <file> -resize 2400x1350! <file>') elsewhere.
- After saving, print a single line: FINAL_IMAGE_PATH=<absolute path to the saved PNG>
- Style: flat-illustration

Generated image: four rounded rectangles centered horizontally at x ≈ 334, 912, 1477, 2063, spanning y ≈ 432–903 (height 471, width 383–417). Pill labels land at y=667 (nodes 1/3) and y=729 (nodes 2/4) — within all four boxes on the y-axis. On the x-axis, all Latin and CJK pills fit inside their respective boxes; the longest Arabic pill (`المعالجة المسبقة`) extends ~28px beyond the second box's edges and is the only minor horizontal overflow.